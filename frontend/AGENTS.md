# Pull Request: Step 1 - Mock Data Preparation

## 目的
MVP（授業一覧 → 授業詳細 → 先輩コメント表示）の実装に向けた準備として、表示用のテストデータ（モックデータ）を定義し、開発を円滑に進める。

## 変更内容
- **`src/data/mockCourses.ts` の新規作成**
  - アプリ内で使用するサンプルデータを追加。
  - `Course` 型に準拠した、プログラミング、心理学、英語の3つの授業データを定義。
  - 各データには、教授名、単位数、説明文、および先輩のコメント（評価・内容・投稿日）を含めています。

## 確認方法
- `npm run build` を実行し、型の不一致やビルドエラーが発生しないことを確認。
- コード上で `src/types/course.ts` の定義と矛盾がないことを目視で確認。

## スクリーンショット
（※今回はデータ定義ファイルのみの追加であり、画面に変更はないため、コードの抜粋を掲載するか、このセクションは「なし」となります）

## 次にやること
- ステップ2：作成したモックデータを使用して、`src/views/CourseListView.vue` に授業一覧を表示する機能を実装する。
